---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-test
  namespace: openebs
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "openebs-kernel-nfs"
  volumeName: nfs-test
  resources:
    requests:
      storage: 100Gi
