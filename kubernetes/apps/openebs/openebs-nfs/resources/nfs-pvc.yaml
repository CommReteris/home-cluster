---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-test
  namespace: openebs
spec:
  accessModes:
    - ReadWriteMany
  #volumeName: nfs-test
  resources:
    requests:
      storage: 100Gi
  storageClassName: openebs-kernel-nfs
